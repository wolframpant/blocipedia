.container
  %h1 Image: "#{@image.name}" for "#{@wiki.title}"

  .image
    .row
      .col-md-11

        = image_tag(@image.location.url)
      
      .col-md-1
        = link_to 'Back to Wiki', wiki_path(id:@image.wiki_id)
        