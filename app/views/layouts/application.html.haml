!!!
%html
  %head
    %title Blocipedia
    = stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true
    = javascript_include_tag "application", "data-turbolinks-track" => true
    = csrf_meta_tags

  %body

    .container
      %ul.nav.nav-tabs
        %li
          = link_to "Blocipedia", root_path
        %li
          = link_to "View Public Wikis", wikis_path
        - if current_user
          %li
            = link_to "View My Wikis", mine_path
          %li
            = link_to "Create Public Wiki", new_wiki_path
        -if current_user && !current_user.premium?
          %li
            = link_to "Become a PREMIUM member!", new_charge_path
        -if current_user && current_user.premium?
          %li
            = link_to "Create Private Wiki", private_wiki_new_path
          %li
            = link_to "View Private Wikis", private_wiki_index_path

        .pull-right.user-info
          - if current_user
            =link_to current_user.name || current_user.email, edit_user_registration_path
            =link_to edit_user_registration_path, class: 'btn btn-primary bt-sm' do
              %span.glyphicon.glyphicon-user
            = link_to destroy_user_session_path, method: :delete, class: 'btn btn-primary btn-sm' do
              %span.glyphicon.glyphicon-log-out
          - else
            = link_to "Sign In", new_user_session_path 
            &nbsp;
            = link_to "Sign Up (free)", new_user_registration_path
      
  
    .notices
      %p= flash[:notice] if flash[:notice]


    = yield 

    